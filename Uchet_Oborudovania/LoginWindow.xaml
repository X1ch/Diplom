<Window x:Class="Uchet_Oborudovania.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация в системе учета оборудования" 
        Height="418" 
        Width="548"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </Window.Resources>

    <Border Background="White" Margin="10" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <Border Grid.Row="0" Background="#2a3f54" CornerRadius="8,8,0,0" Padding="20">
                <TextBlock Text="Вход в систему" 
                           FontSize="22" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           HorizontalAlignment="Center"/>
            </Border>

            <!-- Форма входа -->
            <StackPanel Grid.Row="1" Margin="30,30,30,10">
                <TextBlock Text="Логин" 
                           FontSize="14" 
                           FontWeight="SemiBold" 
                           Foreground="#333333"/>
                <TextBox x:Name="txtLogin" 
                         Style="{StaticResource ModernTextBox}"
                         ToolTip="Введите ваш логин"/>

                <TextBlock Text="Пароль" 
                           FontSize="14" 
                           FontWeight="SemiBold" 
                           Foreground="#333333"
                           Margin="0,10,0,0"/>
                <PasswordBox x:Name="txtPassword" 
                             Style="{StaticResource ModernPasswordBox}"
                             ToolTip="Введите ваш пароль"/>
            </StackPanel>

            <!-- Кнопки -->
            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center" 
                        Margin="0,0,0,20">
                <Button x:Name="btnLogin" 
                        Content="Войти" 
                        Style="{StaticResource ModernButton}"
                        Background="#4CAF50"
                        Click="BtnLogin_Click"
                        Margin="0,0,15,0"/>
                <Button x:Name="btnExit" 
                        Content="Выход" 
                        Style="{StaticResource ModernButton}"
                        Background="#F44336"
                        Click="BtnExit_Click"/>
            </StackPanel>

            <!-- Сообщение об ошибке -->
            <TextBlock x:Name="txtError" 
                       Grid.Row="3"
                       Text="" 
                       Foreground="#F44336" 
                       HorizontalAlignment="Center" 
                       FontSize="12"
                       Margin="20,0,20,10"
                       TextWrapping="Wrap"
                       TextAlignment="Center"/>
        </Grid>
    </Border>
</Window>